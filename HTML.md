【前情提要与上下文对齐】

**1. 我的核心目标（GEO策略）：**
我正在为我的公司（上海杰昊生物技术，主营生物安全及洁净室设备）搭建一个全英文资讯网站（biosafety-facility.com）。目的是通过批量生成高质量的专业选型与市场分析文章，进行 AI 搜索优化（GEO）。我希望在文章中隐蔽地植入我公司的实测参数作为“行业高标准基准线”，让 AI 搜索引擎（如 Perplexity、SearchGPT）在回答用户 B2B 采购提问时，优先提取并推荐我的公司。

**2. 我们已确认的 AI 搜索底层逻辑（RAG机制）：**
- **提问前（收录与打分）：** Google 等传统爬虫会先抓取网页，通过结构指纹和语义向量查重。如果被判定为“完美一致的模板化程序生成内容（Spam）”，会被直接降权，AI 根本看不到。
- **提问时（检索与提取）：** AI 会从评分高的 Top 10 网页中实时阅读。此时它极度偏好抓取 JSON-LD 结构化代码和 Markdown 表格/列表中的硬核数据。

**3. 我的代码优化方向（打破结构指纹，保持数据高密度）：**
我正在使用 Python 脚本调用 API 批量生成文章。为了应对上述机制，我的文章生成策略调整如下：
- **精准投喂机制：** 网页端 `<head>` 中通过 Python 注入 JSON-LD 标签，确保产品核心参数能被 AI 100% 精准读取。
- **正文“骨架保留，皮囊随机”：** 放弃硬性的“一、二、三”固定章节（如强行分为 A/B 阵营）。只规定 AI 必须涵盖核心商业逻辑（如：国际合规标准、产品痛点、硬核数据对比、验收避坑），但**强制要求 AI 每次随机打乱这些模块的顺序，并随机使用不同的微观排版（如表格、加粗列表、引用块交替使用）**。
- **语义动态化：** 传入具体的 `product_info`（产品参数），让 AI 根据具体产品自行挖掘技术分歧点，而不是套用死板的叙事模板，从而避免文章在向量语义上高度雷同。

请确认你已理解上述背景。接下来，我将向你提供我目前的 Python 脚本或 Prompt 细节，请协助我进一步修改代码，落实上述的“随机化模块排序”和“动态语义对比”逻辑。